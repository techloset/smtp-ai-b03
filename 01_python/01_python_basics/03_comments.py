# We use comments to explain our code so other people (and our future self) can understand it easily.
# How to write comments in Python.

# This is a single-line comment.
# Python ignores everything after the # on this line.

print("This line will run")  # This comment explains the print

"""
This is a multi-line comment (technically, a string that we do not use).
We can use it as a block comment or simple documentation.
"""

print("Comments make your code easier to understand!")

# This is a multi-line comment (technically, a string that we do not use).
# We can use it as a block comment or simple documentation.
# This is a multi-line comment (technically, a string that we do not use).

"""
This is a multi-line comment (technically, a string that we do not use).
We can use it as a block comment or simple documentation.
This is a multi-line comment (technically, a string that we do not use).
We can use it as a block comment or simple documentation.
This is a multi-line comment (technically, a string that we do not use).
"""